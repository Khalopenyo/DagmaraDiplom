---
phase: 02-dashboard-rates-context
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/demo/types.ts
  - src/demo/accountSummary.ts
  - src/demo/quickActions.ts
  - src/demo/cbdcRates.ts
  - src/demo/formatters.ts
  - src/demo/index.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - RATE-01
  - RATE-02
  - RATE-03
user_setup: []
must_haves:
  truths:
    - Phase 2 reads exact seeded account, quick-action, and rates values from one shared frontend-only source of truth instead of duplicating literals inside route pages.
    - The account owner, masked number, starting balance, and three country rates are encoded as typed local demo data ready for reuse in later phases.
    - Dashboard and rates implementations can import the same demo modules without adding network calls, reducer state, or backend-shaped abstractions.
  artifacts:
    - path: "src/demo/accountSummary.ts"
      provides: "Typed seeded dashboard account data for Dagmara"
      contains: "4756 •••• •••• 9018"
    - path: "src/demo/quickActions.ts"
      provides: "Eight fixed quick-action items with one route-enabled transfer action"
      contains: "Аккаунт и счета"
    - path: "src/demo/cbdcRates.ts"
      provides: "Three-row simulated CBDC rates directory with China first"
      contains: "2.234"
    - path: "src/demo/formatters.ts"
      provides: "Shared display helpers for Phase 2 financial labels"
      contains: "ЦР"
    - path: "src/demo/index.ts"
      provides: "Stable barrel exports for later page and feature imports"
      contains: "export"
  key_links:
    - from: "src/demo/index.ts"
      to: "src/demo/accountSummary.ts"
      via: "shared demo barrel export"
      pattern: "accountSummary"
    - from: "src/demo/index.ts"
      to: "src/demo/quickActions.ts"
      via: "shared demo barrel export"
      pattern: "quickActions"
    - from: "src/demo/index.ts"
      to: "src/demo/cbdcRates.ts"
      via: "shared demo barrel export"
      pattern: "cbdcRates"
---

<objective>
Create the shared typed demo-data layer that Phase 2 pages will render from.

Purpose: Dashboard and rates UI should be built on a single frontend-only source of truth, not duplicated literals or fake API plumbing.
Output: Typed account, quick-action, and rates seed modules plus small formatting helpers and a barrel export.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/research/ARCHITECTURE.md
@/Users/tkestkes/Dagmara/.planning/research/FEATURES.md
@/Users/tkestkes/Dagmara/.planning/phases/02-dashboard-rates-context/02-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/02-dashboard-rates-context/02-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/02-dashboard-rates-context/02-VALIDATION.md
@/Users/tkestkes/Dagmara/.planning/phases/02-dashboard-rates-context/02-UI-SPEC.md
@/Users/tkestkes/Dagmara/src/content/topLevelRoutes.ts
@/Users/tkestkes/Dagmara/src/content/demoCopy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed account and quick-action demo contracts</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/02-dashboard-rates-context/02-CONTEXT.md
.planning/phases/02-dashboard-rates-context/02-RESEARCH.md
.planning/phases/02-dashboard-rates-context/02-UI-SPEC.md
src/demo/types.ts
src/demo/accountSummary.ts
src/demo/quickActions.ts
  </read_first>
  <files>
src/demo/types.ts
src/demo/accountSummary.ts
src/demo/quickActions.ts
  </files>
  <action>Create `src/demo/types.ts` with concrete Phase 2 data contracts for one seeded account summary, one quick-action item list, and one rates row. In `src/demo/accountSummary.ts`, export one exact seeded account for `Дагмара` with `maskedAccountNumber: "4756 •••• •••• 9018"`, `balanceAmount: 3469.52`, and `currencyLabel: "ЦР"`. In `src/demo/quickActions.ts`, export exactly eight items with labels `Аккаунт и счета`, `Переводы`, `Снятие`, `Оплата счета`, `Накопления`, `Кредитная карта`, `Отчет о транзакциях`, and `Контакты`; only the `Переводы` item should use `mode: "route"` and `to: "/transfers"`, while the remaining seven use `mode: "display"`. Keep these modules frontend-only and synchronous: no fetches, promises, or reducer wiring.</action>
  <acceptance_criteria>
    - `src/demo/accountSummary.ts` contains `Дагмара`, `4756 •••• •••• 9018`, `3469.52`, and `ЦР`.
    - `src/demo/quickActions.ts` contains all eight required quick-action labels and exactly one `/transfers` target.
    - `src/demo/types.ts` contains concrete exported types for the account summary and quick-action contracts.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>The repo has typed seeded account and quick-action data that downstream pages can import without duplicating literals.</done>
</task>

<task type="auto">
  <name>Task 2: Create seeded rates data, formatting helpers, and barrel exports</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/phases/02-dashboard-rates-context/02-CONTEXT.md
.planning/phases/02-dashboard-rates-context/02-RESEARCH.md
.planning/phases/02-dashboard-rates-context/02-UI-SPEC.md
src/demo/types.ts
src/demo/cbdcRates.ts
src/demo/formatters.ts
src/demo/index.ts
  </read_first>
  <files>
src/demo/cbdcRates.ts
src/demo/formatters.ts
src/demo/index.ts
  </files>
  <action>Create `src/demo/cbdcRates.ts` as the exact three-row simulated rates source with China first: `Китай -> 2.234`, `Вьетнам -> 1.746`, `Южная Корея -> 5.151`. Each entry must also include `baseLabel: "1 ЦР"`, a `corridor` mode of `"primary"` for China and `"reference"` for the other two rows, plus a country marker string or badge token that later UI can render without remote assets. Add `src/demo/formatters.ts` with a concrete helper that formats `3469.52` and `ЦР` as the display string `3 469.52 ЦР`, and keep the helper suitable for reuse by later transfer and receipt phases. Add `src/demo/index.ts` that re-exports `accountSummary`, `quickActions`, `cbdcRates`, and the formatter helper from one barrel.</action>
  <acceptance_criteria>
    - `src/demo/cbdcRates.ts` contains `Китай`, `Вьетнам`, `Южная Корея`, `2.234`, `1.746`, `5.151`, and `1 ЦР`.
    - `src/demo/cbdcRates.ts` contains `primary` for China and `reference` for the other corridor rows.
    - `src/demo/formatters.ts` contains the exact formatted output target `3 469.52 ЦР`.
    - `src/demo/index.ts` re-exports `accountSummary`, `quickActions`, `cbdcRates`, and the formatter helper.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>The repo has a reusable Phase 2 demo-data layer for dashboard and rates plus one shared formatting entrypoint.</done>
</task>

</tasks>

<verification>
Run:
- `npm exec tsc --noEmit`
</verification>

<success_criteria>
- Dashboard and rates pages can import one shared frontend-only source of truth.
- The exact seeded account and rate values are encoded once and ready for Phase 2 rendering.
- No backend-shaped or async data layer is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-rates-context/02-01-SUMMARY.md`
</output>
