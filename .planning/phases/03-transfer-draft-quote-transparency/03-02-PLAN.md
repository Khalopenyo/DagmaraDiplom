---
phase: 03-transfer-draft-quote-transparency
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/features/transfers/TransferDraftForm.tsx
  - src/features/transfers/TransferTypeSelector.tsx
  - src/features/transfers/FavoriteRecipientsStrip.tsx
  - src/features/transfers/favoriteRecipientBadges.tsx
  - src/pages/TransfersPage.tsx
  - src/pages/TransfersPage.test.tsx
  - src/content/topLevelRoutes.ts
autonomous: true
requirements:
  - XFER-01
  - XFER-02
  - XFER-03
  - XFER-04
user_setup: []
must_haves:
  truths:
    - User opening `/transfers` sees a real editable transfer draft form instead of the generic placeholder.
    - User can inspect one seeded source account, switch between card and phone modes, and see the recipient field follow the active mode.
    - User sees the favorites strip with Emma, Justin, and `+`, and favorite selection can prefill the current identifier while keeping it editable.
  artifacts:
    - path: "src/features/transfers/TransferDraftForm.tsx"
      provides: "Main editable transfer draft surface with source account, recipient field, and debit amount"
      contains: "Счет списания"
    - path: "src/features/transfers/TransferTypeSelector.tsx"
      provides: "Two-option mode selector for card and phone transfer entry"
      contains: "По номеру телефона"
    - path: "src/features/transfers/FavoriteRecipientsStrip.tsx"
      provides: "Seeded favorites strip for Emma, Justin, and add action"
      contains: "Emma"
    - path: "src/pages/TransfersPage.tsx"
      provides: "Phase 3 transfer route replacing the generic placeholder"
      contains: "Сумма списания"
    - path: "src/pages/TransfersPage.test.tsx"
      provides: "Route-level coverage for transfer form structure and mode-aware recipient entry"
      contains: "Justin"
  key_links:
    - from: "src/pages/TransfersPage.tsx"
      to: "src/features/transfers/TransferDraftForm.tsx"
      via: "route composition for the editable transfer surface"
      pattern: "TransferDraftForm"
    - from: "src/features/transfers/TransferDraftForm.tsx"
      to: "src/demo/index.ts"
      via: "imports source account, transfer mode, and favorite recipient seeds"
      pattern: "transferSourceAccounts|transferModes|favoriteRecipients"
    - from: "src/pages/TransfersPage.test.tsx"
      to: "src/pages/TransfersPage.tsx"
      via: "transfer route verification"
      pattern: "TransfersPage"
---

<objective>
Replace the generic transfers placeholder with the real Phase 3 editable draft form surface.

Purpose: The MVP needs a credible writable transfer route before quote transparency and processing can be demonstrated.
Output: Transfer form components, transfer route implementation, updated route metadata, and route-level interaction tests for account/mode/favorites behavior.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-VALIDATION.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-01-PLAN.md
@/Users/tkestkes/Dagmara/src/pages/TransfersPage.tsx
@/Users/tkestkes/Dagmara/src/pages/RoutePlaceholderPage.tsx
@/Users/tkestkes/Dagmara/src/content/topLevelRoutes.ts
@/Users/tkestkes/Dagmara/src/content/demoCopy.ts
@/Users/tkestkes/Dagmara/src/shell/ShellCard.tsx
@/Users/tkestkes/Dagmara/src/demo/index.ts
@/Users/tkestkes/Dagmara/src/test/renderApp.tsx
@/Users/tkestkes/Dagmara/src/app/AppRoutes.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the real transfer draft route layout and editable form surface</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
src/pages/TransfersPage.tsx
src/pages/RoutePlaceholderPage.tsx
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/shell/ShellCard.tsx
src/demo/index.ts
src/features/transfers/TransferDraftForm.tsx
src/features/transfers/TransferTypeSelector.tsx
src/features/transfers/FavoriteRecipientsStrip.tsx
  </read_first>
  <files>
src/features/transfers/TransferDraftForm.tsx
src/features/transfers/TransferTypeSelector.tsx
src/features/transfers/FavoriteRecipientsStrip.tsx
src/features/transfers/favoriteRecipientBadges.tsx
src/pages/TransfersPage.tsx
src/content/topLevelRoutes.ts
  </files>
  <action>Replace `RoutePlaceholderPage` in `src/pages/TransfersPage.tsx` with a real Phase 3 page stack that keeps the intro-card simulation boundary and uses updated route metadata from `getTopLevelRoute('/transfers')`. Update the `/transfers` entry in `src/content/topLevelRoutes.ts` so the supporting copy explicitly describes collecting a draft for corridor `Россия → Китай` instead of Phase 1 placeholder language. Create `src/features/transfers/TransferDraftForm.tsx` as the main editable card and render, in order, a source-account section labeled `Счет списания` with one seeded Dagmara account and visible available balance `3 469.52 ЦР`, a transfer-type selector with `По номеру карты` and `По номеру телефона`, a favorites strip with `Emma`, `Justin`, and `+`, one always-visible recipient identifier field, and one debit amount field labeled `Сумма списания`. Create lightweight local avatar/badge treatment in `src/features/transfers/favoriteRecipientBadges.tsx`. Keep all state local to the route or form component: selected mode, selected favorite, recipient identifier, and debit amount string. Do not add submit side effects, transaction IDs, trackers, or receipts in this plan.</action>
  <acceptance_criteria>
    - `src/pages/TransfersPage.tsx` does not contain `RoutePlaceholderPage`.
    - `src/pages/TransfersPage.tsx` contains `PHASE_BOUNDARY_COPY` or imports the canonical boundary line from `src/content/demoCopy.ts`.
    - `src/features/transfers/TransferDraftForm.tsx` contains `Счет списания`, `Сумма списания`, and `3 469.52 ЦР`.
    - `src/features/transfers/TransferTypeSelector.tsx` contains `По номеру карты` and `По номеру телефона`.
    - `src/features/transfers/FavoriteRecipientsStrip.tsx` contains `Emma`, `Justin`, and `+`.
    - `src/content/topLevelRoutes.ts` contains updated `/transfers` supporting copy that no longer says Phase 1 should show only a placeholder frame.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>The transfer route renders a real editable draft surface inside the shell instead of the generic placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route-level tests for source account, transfer mode, and favorite-recipient behavior</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
.planning/phases/03-transfer-draft-quote-transparency/03-VALIDATION.md
.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
src/demo/index.ts
src/pages/TransfersPage.tsx
src/pages/TransfersPage.test.tsx
src/test/renderApp.tsx
src/app/AppRoutes.tsx
src/features/transfers/TransferDraftForm.tsx
src/features/transfers/TransferTypeSelector.tsx
src/features/transfers/FavoriteRecipientsStrip.tsx
  </read_first>
  <files>
src/pages/TransfersPage.test.tsx
  </files>
  <behavior>
- Test 1: `/transfers` renders the seeded source account and the visible available balance before confirmation.
- Test 2: The page shows both transfer modes and switching modes changes the recipient label between card and phone.
- Test 3: The favorites strip shows `Emma`, `Justin`, and `+`, and selecting a favorite prefills the current recipient identifier while keeping the field editable.
  </behavior>
  <action>Write `src/pages/TransfersPage.test.tsx` first using the existing router-aware render helper and route tree. Make the test suite assert the exact seeded account number `4756 •••• •••• 9018`, available balance `3 469.52 ЦР`, the two transfer modes, and the favorites strip labels `Emma`, `Justin`, and `+`. Add one interaction assertion that switching from card mode to phone mode changes the visible recipient field label from `Номер карты получателя` to `Номер телефона получателя`. Add one interaction assertion that selecting `Emma` or `Justin` prefills the current field value while the input still accepts subsequent user typing. Keep the tests route-level and user-visible; do not inspect implementation-only classnames.</action>
  <acceptance_criteria>
    - `src/pages/TransfersPage.test.tsx` contains `4756 •••• •••• 9018`, `3 469.52 ЦР`, `По номеру карты`, `По номеру телефона`, `Emma`, `Justin`, and `+`.
    - `src/pages/TransfersPage.test.tsx` contains an assertion that switching modes changes the recipient field label.
    - `src/pages/TransfersPage.test.tsx` contains an assertion that favorite selection prefills the current recipient identifier and the input remains editable.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/pages/TransfersPage.test.tsx --run</automated>
  </verify>
  <done>The transfer route proves its draft-form surface, mode switching, and favorite-recipient prefilling by route-level test.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- src/pages/TransfersPage.test.tsx --run`
- `npm exec tsc --noEmit`
</verification>

<success_criteria>
- `/transfers` shows a real editable draft form with source account, transfer modes, favorites, recipient input, and debit amount input.
- Favorite recipients accelerate the demo without hiding manual entry.
- The transfer page no longer depends on generic placeholder copy or structure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-transfer-draft-quote-transparency/03-02-SUMMARY.md`
</output>
