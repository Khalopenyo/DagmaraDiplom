---
phase: 03-transfer-draft-quote-transparency
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/features/transfers/QuotePreviewCard.tsx
  - src/features/transfers/TransferDraftForm.tsx
  - src/pages/TransfersPage.tsx
  - src/pages/TransfersPage.test.tsx
autonomous: true
requirements:
  - XFER-05
  - XFER-06
  - XFER-07
user_setup: []
must_haves:
  truths:
    - User editing the debit amount sees the recipient amount recalculate automatically from the locked China corridor rate.
    - User sees a full simulated quote preview before confirmation, including rate, debit amount, recipient amount, platform fee, and total.
    - `Подтвердить` stays disabled while the draft is invalid or over balance and does not start tracker or receipt behavior in Phase 3.
  artifacts:
    - path: "src/features/transfers/QuotePreviewCard.tsx"
      provides: "Derived quote summary card for the locked Russia -> China corridor"
      contains: "Комиссия платформы"
    - path: "src/features/transfers/TransferDraftForm.tsx"
      provides: "Debit amount source-of-truth wired into quote and validation logic"
      contains: "Сумма списания"
    - path: "src/pages/TransfersPage.tsx"
      provides: "Final Phase 3 transfer route with draft form plus quote preview"
      contains: "Подтвердить"
    - path: "src/pages/TransfersPage.test.tsx"
      provides: "Regression coverage for recalculation, breakdown visibility, and CTA gating"
      contains: "223.40 ¥"
  key_links:
    - from: "src/features/transfers/TransferDraftForm.tsx"
      to: "src/demo/transferQuote.ts"
      via: "wires debit and identifier state into deterministic quote helpers"
      pattern: "buildTransferQuote|validateTransferDraft"
    - from: "src/pages/TransfersPage.tsx"
      to: "src/features/transfers/QuotePreviewCard.tsx"
      via: "page composition for quote transparency"
      pattern: "QuotePreviewCard"
    - from: "src/pages/TransfersPage.test.tsx"
      to: "src/pages/TransfersPage.tsx"
      via: "transfer quote verification"
      pattern: "TransfersPage"
---

<objective>
Add quote transparency and draft-validity gating to the real transfer route.

Purpose: The MVP needs to show how the debit amount becomes a recipient amount and why an invalid draft cannot advance, before Phase 4 introduces actual processing.
Output: Quote preview card, derived amount wiring, CTA disabled-state rules, and route-level coverage for recalculation and confirm gating.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-VALIDATION.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-01-PLAN.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-02-PLAN.md
@/Users/tkestkes/Dagmara/src/pages/TransfersPage.tsx
@/Users/tkestkes/Dagmara/src/demo/index.ts
@/Users/tkestkes/Dagmara/src/demo/transferQuote.ts
@/Users/tkestkes/Dagmara/src/features/transfers/TransferDraftForm.tsx
@/Users/tkestkes/Dagmara/src/pages/TransfersPage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the quote preview card and wire draft state into deterministic quote helpers</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
src/pages/TransfersPage.tsx
src/demo/index.ts
src/demo/transferQuote.ts
src/features/transfers/TransferDraftForm.tsx
src/features/transfers/QuotePreviewCard.tsx
  </read_first>
  <files>
src/features/transfers/QuotePreviewCard.tsx
src/features/transfers/TransferDraftForm.tsx
src/pages/TransfersPage.tsx
  </files>
  <action>Create `src/features/transfers/QuotePreviewCard.tsx` as the derived summary surface for the locked corridor and render it alongside the existing transfer form on `/transfers`. The card must display a simulated quote badge for `Россия → Китай`, the exact rate line `1 ЦР = 2.234 ЦЮ`, and the breakdown rows `Сумма списания`, `Сумма получения`, `Комиссия платформы`, and `Итого`. Wire the route's local draft state into `buildTransferQuote` and `validateTransferDraft` so the recipient amount, fee, and total update immediately when the debit amount changes. Show recipient amount with the exact `¥` display formatting and keep the fee fixed at `10 ₽`. Render the primary CTA `Подтвердить` inside the quote card, but keep it local-only in Phase 3: no navigation, no transaction creation, and no status simulation side effects.</action>
  <acceptance_criteria>
    - `src/features/transfers/QuotePreviewCard.tsx` contains `Россия → Китай`, `1 ЦР = 2.234 ЦЮ`, `Комиссия платформы`, and `Итого`.
    - `src/pages/TransfersPage.tsx` contains `Подтвердить` and composes `QuotePreviewCard`.
    - `src/features/transfers/TransferDraftForm.tsx` imports or receives data derived from `buildTransferQuote` and `validateTransferDraft`.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>The transfer route shows a real derived quote preview card wired to the form state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend route tests for amount recalculation and confirm gating</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
.planning/phases/03-transfer-draft-quote-transparency/03-VALIDATION.md
.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
src/demo/transferQuote.ts
src/pages/TransfersPage.tsx
src/pages/TransfersPage.test.tsx
src/test/renderApp.tsx
src/app/AppRoutes.tsx
src/features/transfers/QuotePreviewCard.tsx
  </read_first>
  <files>
src/pages/TransfersPage.test.tsx
  </files>
  <behavior>
- Test 1: entering a debit amount of `100` recalculates the quote to `223.40 ¥`, fee `10 ₽`, and total `110 ₽`.
- Test 2: the route shows the exact breakdown labels and the rate line `1 ЦР = 2.234 ЦЮ` before confirmation.
- Test 3: `Подтвердить` is disabled when required fields are missing or the debit amount exceeds `3 469.52`, and becomes enabled only for a valid draft.
  </behavior>
  <action>Extend `src/pages/TransfersPage.test.tsx` first so it drives the real route through the full quote path. Make the suite enter `100` into `Сумма списания` and assert the user-visible quote outputs `223.40 ¥`, `10 ₽`, and `110 ₽` together with the exact rate line `1 ЦР = 2.234 ЦЮ`. Add one invalid-state assertion where `Подтвердить` stays disabled while the recipient field is empty or the debit amount exceeds `3469.52`. Add one valid-state assertion where a correctly formatted identifier plus an in-balance debit amount enables `Подтвердить`, but clicking it keeps the user on `/transfers` because Phase 4 has not started yet.</action>
  <acceptance_criteria>
    - `src/pages/TransfersPage.test.tsx` contains `223.40 ¥`, `10 ₽`, `110 ₽`, and `1 ЦР = 2.234 ЦЮ`.
    - `src/pages/TransfersPage.test.tsx` contains an assertion that `Подтвердить` is disabled for invalid or over-balance drafts.
    - `src/pages/TransfersPage.test.tsx` contains an assertion that `Подтвердить` becomes enabled for a valid draft without navigating away from `/transfers`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/pages/TransfersPage.test.tsx --run</automated>
  </verify>
  <done>The transfer route proves its deterministic quote math and confirm gating through route-level regression tests.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- src/pages/TransfersPage.test.tsx --run`
- `npm exec tsc --noEmit`
</verification>

<success_criteria>
- `/transfers` recalculates the recipient amount automatically from the locked China rate.
- The quote breakdown is visible before confirmation and matches the deterministic fee policy.
- `Подтвердить` is unavailable for incomplete or over-balance drafts and remains local-only in Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/03-transfer-draft-quote-transparency/03-03-SUMMARY.md`
</output>
