---
phase: 03-transfer-draft-quote-transparency
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/demo/types.ts
  - src/demo/transferOptions.ts
  - src/demo/favoriteRecipients.ts
  - src/demo/transferQuote.ts
  - src/demo/transferQuote.test.ts
  - src/demo/index.ts
autonomous: true
requirements:
  - XFER-01
  - XFER-02
  - XFER-03
  - XFER-04
  - XFER-05
  - XFER-06
  - XFER-07
user_setup: []
must_haves:
  truths:
    - Phase 3 reads transfer source-account, transfer-mode, favorite-recipient, fixed-fee, and China-rate values from one shared frontend-only source of truth.
    - Quote math derives recipient amount, fee, and total from one debit amount without duplicating financial literals inside route components.
    - Draft validation can reject empty recipient data, mode-mismatched identifiers, and overdraft attempts before Phase 4 submit logic exists.
  artifacts:
    - path: "src/demo/transferOptions.ts"
      provides: "Seeded transfer account, transfer modes, and locked corridor quote labels"
      contains: "По номеру карты"
    - path: "src/demo/favoriteRecipients.ts"
      provides: "Seeded favorite recipients for the transfer route"
      contains: "Emma"
    - path: "src/demo/transferQuote.ts"
      provides: "Deterministic quote math and validation helpers for Phase 3"
      contains: "2.234"
    - path: "src/demo/transferQuote.test.ts"
      provides: "Unit coverage for recipient amount, fee, total, and invalid draft states"
      contains: "223.40"
    - path: "src/demo/index.ts"
      provides: "Stable barrel exports for transfer draft modules"
      contains: "transferQuote"
  key_links:
    - from: "src/demo/index.ts"
      to: "src/demo/transferOptions.ts"
      via: "shared demo barrel export"
      pattern: "transferSourceAccounts|transferModes"
    - from: "src/demo/index.ts"
      to: "src/demo/favoriteRecipients.ts"
      via: "shared demo barrel export"
      pattern: "favoriteRecipients"
    - from: "src/demo/transferQuote.test.ts"
      to: "src/demo/transferQuote.ts"
      via: "unit verification of deterministic quote helpers"
      pattern: "buildTransferQuote|validateTransferDraft"
---

<objective>
Create the shared typed transfer-draft data layer and deterministic quote helpers for Phase 3.

Purpose: `/transfers` should build on one local source of truth for account, recipients, modes, fee, rate, and validation rather than scattering form assumptions inside components.
Output: Transfer seed modules, quote/validation helpers, unit coverage, and stable barrel exports.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-VALIDATION.md
@/Users/tkestkes/Dagmara/.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
@/Users/tkestkes/Dagmara/.planning/phases/02-dashboard-rates-context/02-VERIFICATION.md
@/Users/tkestkes/Dagmara/src/demo/types.ts
@/Users/tkestkes/Dagmara/src/demo/index.ts
@/Users/tkestkes/Dagmara/src/demo/accountSummary.ts
@/Users/tkestkes/Dagmara/src/demo/cbdcRates.ts
@/Users/tkestkes/Dagmara/src/demo/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed transfer seeds for account selection, transfer modes, and favorite recipients</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
.planning/phases/03-transfer-draft-quote-transparency/03-UI-SPEC.md
src/demo/types.ts
src/demo/index.ts
src/demo/accountSummary.ts
src/demo/cbdcRates.ts
src/demo/transferOptions.ts
src/demo/favoriteRecipients.ts
  </read_first>
  <files>
src/demo/types.ts
src/demo/transferOptions.ts
src/demo/favoriteRecipients.ts
src/demo/index.ts
  </files>
  <action>Extend `src/demo/types.ts` with concrete Phase 3 transfer contracts: one source-account type, one transfer-mode type with IDs `card` and `phone`, one favorite-recipient type with both card and phone prefills, and one quote/validation result type. Create `src/demo/transferOptions.ts` that exports exactly one executable source account based on Dagmara's seeded account, plus two transfer modes with labels `По номеру карты` and `По номеру телефона`, input labels `Номер карты получателя` and `Номер телефона получателя`, placeholders `2200 0000 0000 0000` and `+86 138 0000 0000`, and the exact locked rate label `1 ЦР = 2.234 ЦЮ`. Create `src/demo/favoriteRecipients.ts` that exports exactly two favorites named `Emma` and `Justin`, plus one display-first add label `+`; both favorites must include explicit `cardValue` and `phoneValue` prefills that later UI can inject without fetches or modal flows. Update `src/demo/index.ts` to re-export all new transfer modules.</action>
  <acceptance_criteria>
    - `src/demo/transferOptions.ts` contains `По номеру карты`, `По номеру телефона`, `Номер карты получателя`, `Номер телефона получателя`, and `1 ЦР = 2.234 ЦЮ`.
    - `src/demo/favoriteRecipients.ts` contains `Emma`, `Justin`, and `+`.
    - `src/demo/types.ts` contains exported transfer contracts for source account, transfer mode, favorite recipient, and quote/validation results.
    - `src/demo/index.ts` re-exports `transferSourceAccounts`, `transferModes`, `favoriteRecipients`, and `FAVORITE_RECIPIENT_ADD_LABEL`.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>The repo has typed transfer seed data for source account selection, transfer modes, and favorite recipients.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic quote math and draft validation helpers with unit coverage</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/phases/03-transfer-draft-quote-transparency/03-CONTEXT.md
.planning/phases/03-transfer-draft-quote-transparency/03-RESEARCH.md
.planning/phases/03-transfer-draft-quote-transparency/03-VALIDATION.md
src/demo/accountSummary.ts
src/demo/cbdcRates.ts
src/demo/transferOptions.ts
src/demo/transferQuote.ts
src/demo/transferQuote.test.ts
  </read_first>
  <files>
src/demo/transferQuote.ts
src/demo/transferQuote.test.ts
  </files>
  <behavior>
- Test 1: a debit amount of `100` produces a recipient amount display of `223.40 ¥`.
- Test 2: the helper exposes a fixed platform fee of `10 ₽` and a total of `110 ₽`.
- Test 3: validation fails when the recipient identifier is empty, mismatched with the active mode, or the debit amount exceeds `3469.52`.
  </behavior>
  <action>Write `src/demo/transferQuote.test.ts` first. Then create `src/demo/transferQuote.ts` exporting the exact constants `LOCKED_CHINA_RATE = 2.234`, `LOCKED_CHINA_RATE_LABEL = "1 ЦР = 2.234 ЦЮ"`, and `PLATFORM_FEE_RUBLES = 10`. Implement `buildTransferQuote(debitAmount: number)` so it returns recipient amount, fee, total, and exact display labels where `100` maps to `223.40 ¥`, `10 ₽`, and `110 ₽`. Implement `validateTransferDraft` so it requires a non-empty recipient identifier, accepts only card-number-like input in `card` mode, accepts only international-phone-like input in `phone` mode, rejects zero/negative debit amounts, and rejects any debit amount greater than Dagmara's seeded available balance `3469.52`. Keep these helpers pure and synchronous with no reducer, no fetch, and no router coupling.</action>
  <acceptance_criteria>
    - `src/demo/transferQuote.ts` contains `2.234`, `1 ЦР = 2.234 ЦЮ`, `10`, and `3469.52`.
    - `src/demo/transferQuote.test.ts` contains `223.40`, `110`, and assertions for invalid empty, invalid format, and over-balance states.
    - `src/demo/transferQuote.ts` exports `buildTransferQuote` and `validateTransferDraft`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/demo/transferQuote.test.ts --run</automated>
  </verify>
  <done>The repo has deterministic quote math and validation helpers that later route components can wire without duplicating money logic.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- src/demo/transferQuote.test.ts --run`
- `npm exec tsc --noEmit`
</verification>

<success_criteria>
- Transfer draft UI can import one shared local source of truth for account, recipients, modes, fee, and rate.
- Quote math is deterministic and reusable by later tracker and receipt phases.
- Validation rules exist before any submit-side effects are added.
</success_criteria>

<output>
After completion, create `.planning/phases/03-transfer-draft-quote-transparency/03-01-SUMMARY.md`
</output>
