---
phase: 01-demo-shell-boundaries
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/pages/RoutePlaceholderPage.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/TransfersPage.tsx
  - src/pages/RatesPage.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/NotFoundPage.tsx
  - src/pages/index.ts
  - src/pages/RoutePlaceholders.test.tsx
autonomous: true
requirements:
  - DEMO-01
user_setup: []
must_haves:
  truths:
    - Every Phase 1 top-level route starts with explicit simulated-demo framing so the prototype is not mistaken for a live bank.
    - `Главная` and `Переводы` use the intro-card treatment, while `Обмен валют` and `Настройки` use inline helper copy above the first placeholder card.
    - Unknown routes show the approved error-state card and a CTA back to the dashboard.
  artifacts:
    - path: "src/pages/RoutePlaceholderPage.tsx"
      provides: "Shared route placeholder renderer honoring boundary mode and CTA contract"
      contains: "boundaryMode"
    - path: "src/pages/DashboardPage.tsx"
      provides: "Home route title and primary CTA to transfers"
      contains: "Демонстрационный маршрут Россия → Китай"
    - path: "src/pages/NotFoundPage.tsx"
      provides: "Error-state card with dashboard recovery CTA"
      contains: "Вернуться на главную"
    - path: "src/pages/RoutePlaceholders.test.tsx"
      provides: "Requirement-level assertions for disclaimer visibility and CTA routing"
      contains: "Клиентская симуляция с фиктивными данными."
  key_links:
    - from: "src/pages/RoutePlaceholderPage.tsx"
      to: "src/content/topLevelRoutes.ts"
      via: "Page title, supporting copy, CTA, and boundary mode"
      pattern: "boundaryMode"
    - from: "src/pages/RoutePlaceholderPage.tsx"
      to: "src/content/demoCopy.ts"
      via: "Canonical disclaimer and empty/error copy"
      pattern: "Клиентская симуляция"
    - from: "src/pages/NotFoundPage.tsx"
      to: "/dashboard"
      via: "Recovery CTA"
      pattern: "to=\"/dashboard\""
---

<objective>
Replace the route stubs with real Phase 1 placeholder screens that communicate the simulation boundary clearly and complete the user-visible contract for the desktop demo shell.

Purpose: DEMO-01 is only satisfied when the routed screens themselves carry the approved framing and recovery copy, not just the shell chrome.
Output: Route placeholder pages, shared route placeholder renderer, not-found recovery card, and phase-level content tests.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-02-SUMMARY.md

<interfaces>
Expected route metadata from `src/content/topLevelRoutes.ts`:
```ts
export interface TopLevelRoute {
  path: "/dashboard" | "/transfers" | "/rates" | "/settings";
  navLabel: "Главная" | "Переводы" | "Обмен валют" | "Настройки";
  pageTitle: string;
  supportingCopy: string;
  ctaLabel: string;
  boundaryMode: "intro-card" | "inline-helper";
}
```

Expected shell primitives already available:
```ts
// PageContainer constrains page content to 1200px with an inner 760px narrative column.
// ShellCard renders the approved white 24px-radius card surface.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Phase 1 route placeholders and boundary treatments</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/shell/PageContainer.tsx
src/shell/ShellCard.tsx
src/pages/DashboardPage.tsx
src/pages/TransfersPage.tsx
src/pages/RatesPage.tsx
src/pages/SettingsPage.tsx
src/pages/NotFoundPage.tsx
src/pages/RoutePlaceholderPage.tsx
src/pages/index.ts
  </read_first>
  <files>
src/pages/RoutePlaceholderPage.tsx
src/pages/DashboardPage.tsx
src/pages/TransfersPage.tsx
src/pages/RatesPage.tsx
src/pages/SettingsPage.tsx
src/pages/NotFoundPage.tsx
src/pages/index.ts
  </files>
  <behavior>
- Test 1: `Главная` renders the title `Демонстрационный маршрут Россия → Китай`, the canonical disclaimer, and the CTA `Перейти к переводу`.
- Test 2: `Переводы` renders the exact route title `Переводы`, the same disclaimer, and the CTA `Вернуться на главную`.
- Test 3: `Обмен валют` and `Настройки` render the canonical disclaimer as inline helper text above the first placeholder card instead of inside the accent intro card.
  </behavior>
  <action>Build `src/pages/RoutePlaceholderPage.tsx` as the shared renderer for Phase 1 routed content. It must consume a `TopLevelRoute` record plus the canonical disclaimer copy and render one of two modes from the metadata contract: `intro-card` for `/dashboard` and `/transfers`, `inline-helper` for `/rates` and `/settings`. Use `PageContainer` and `ShellCard` from Plan 02 so the placeholder screens stay inside the centered `1200px` shell and preserve the card-based visual language from D-09. Update `DashboardPage.tsx`, `TransfersPage.tsx`, `RatesPage.tsx`, and `SettingsPage.tsx` so each file selects its route metadata and renders the exact `pageTitle`, `supportingCopy`, and `ctaLabel` from `topLevelRoutes.ts`. The dashboard CTA must link to `/transfers`; the other three route CTAs must link to `/dashboard`. Keep the screen content intentionally descriptive only: no rates table yet, no account widget yet, no transfer form, and no backend-claim language.</action>
  <acceptance_criteria>
- `src/pages/DashboardPage.tsx` contains `Демонстрационный маршрут Россия → Китай`.
- `src/pages/TransfersPage.tsx` contains `Переводы`.
- `src/pages/RatesPage.tsx` contains `Обмен валют`.
- `src/pages/SettingsPage.tsx` contains `Настройки`.
- `src/pages/RoutePlaceholderPage.tsx` contains `boundaryMode`, `Перейти к переводу`, and `Вернуться на главную`.
- All four page files or the shared placeholder component contain the exact canonical disclaimer string from `src/content/demoCopy.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>All four routed pages render the approved Phase 1 placeholder content and demo-boundary treatments without introducing future-phase UI prematurely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the not-found recovery state and phase-level content regression tests</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/content/demoCopy.ts
src/app/AppRoutes.tsx
src/test/renderApp.tsx
src/pages/NotFoundPage.tsx
src/pages/RoutePlaceholders.test.tsx
  </read_first>
  <files>
src/pages/NotFoundPage.tsx
src/pages/RoutePlaceholders.test.tsx
  </files>
  <behavior>
- Test 1: Visiting `/dashboard`, `/transfers`, `/rates`, and `/settings` each shows the canonical disclaimer plus the shell-level `Simulated demo` badge.
- Test 2: The dashboard CTA navigates to `/transfers`, while the other top-level placeholders navigate back to `/dashboard`.
- Test 3: Visiting an unknown route shows the exact error copy `Не удалось отобразить демо-экран. Вернитесь на «Главную» или обновите страницу — реальные данные не затронуты.` and a `Вернуться на главную` CTA to `/dashboard`.
  </behavior>
  <action>Write `src/pages/RoutePlaceholders.test.tsx` first and cover the four top-level routes plus one unknown route using the shared render helper from Plan 01. The tests must assert the exact canonical disclaimer on every routed screen, the presence of the persistent shell badge `Simulated demo`, the correct CTA target for `/dashboard` versus the secondary routes, and the exact not-found error copy from `01-UI-SPEC.md`. Then implement `src/pages/NotFoundPage.tsx` so it renders that approved error card and a `Link` back to `/dashboard`. Keep the not-found view inside the shell and do not expose a raw browser error or blank page.</action>
  <acceptance_criteria>
- `src/pages/RoutePlaceholders.test.tsx` contains `/dashboard`, `/transfers`, `/rates`, `/settings`, and an unknown-route case.
- `src/pages/RoutePlaceholders.test.tsx` contains the exact string `Simulated demo`.
- `src/pages/RoutePlaceholders.test.tsx` contains the exact canonical disclaimer string and the exact not-found error copy from `01-UI-SPEC.md`.
- `src/pages/NotFoundPage.tsx` contains `to="/dashboard"` and `Вернуться на главную`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/pages/RoutePlaceholders.test.tsx --run</automated>
  </verify>
  <done>Phase 1 has user-visible demo framing on all key screens plus automated regression coverage for the route placeholders and not-found recovery flow.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- Every Phase 1 route begins with the exact canonical simulation disclaimer.
- `Главная` and `Переводы` use the intro-card treatment, while `Обмен валют` and `Настройки` use inline helper framing.
- Unknown routes recover inside the shell with the approved error copy and a dashboard CTA.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-03-SUMMARY.md`
</output>
