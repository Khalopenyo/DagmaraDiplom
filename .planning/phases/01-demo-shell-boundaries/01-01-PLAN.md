---
phase: 01-demo-shell-boundaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - eslint.config.js
  - public/vite.svg
  - src/App.tsx
  - src/index.css
  - src/assets/react.svg
  - src/main.tsx
  - src/vite-env.d.ts
  - src/styles/app.css
  - src/app/AppRouter.tsx
  - src/app/AppRoutes.tsx
  - src/content/topLevelRoutes.ts
  - src/content/demoCopy.ts
  - src/pages/DashboardPage.tsx
  - src/pages/TransfersPage.tsx
  - src/pages/RatesPage.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/NotFoundPage.tsx
  - src/pages/index.ts
  - src/test/setup.ts
  - src/test/renderApp.tsx
  - src/app/AppRouter.test.tsx
autonomous: true
requirements:
  - NAVG-03
user_setup: []
must_haves:
  truths:
    - Repository installs as a Vite React SPA without introducing backend code or external service setup.
    - The app has a single router contract for `/dashboard`, `/transfers`, `/rates`, `/settings`, plus a root redirect to `/dashboard` per D-02.
    - Vitest and React Testing Library can render Phase 1 routes in jsdom for later shell and content verification.
  artifacts:
    - path: "package.json"
      provides: "React 19, Vite 8, React Router 7, Tailwind 4, and Vitest toolchain"
      contains: "\"react-router\""
    - path: "src/app/AppRouter.tsx"
      provides: "BrowserRouter wrapper isolated to one file for low-cost HashRouter fallback"
      contains: "BrowserRouter"
    - path: "src/app/AppRoutes.tsx"
      provides: "Shared route tree contract with root redirect and top-level paths"
      contains: "Navigate to=\"/dashboard\""
    - path: "src/content/topLevelRoutes.ts"
      provides: "Single source of truth for Phase 1 route metadata"
      contains: "navLabel"
    - path: "src/test/renderApp.tsx"
      provides: "Reusable route-aware render helper for follow-on plans"
      contains: "MemoryRouter"
  key_links:
    - from: "src/main.tsx"
      to: "src/app/AppRouter.tsx"
      via: "React root render"
      pattern: "createRoot.*<AppRouter />"
    - from: "src/app/AppRouter.tsx"
      to: "src/app/AppRoutes.tsx"
      via: "Provider isolation for BrowserRouter"
      pattern: "<BrowserRouter>.*<AppRoutes />"
    - from: "src/app/AppRoutes.tsx"
      to: "src/content/topLevelRoutes.ts"
      via: "Path definitions consumed from typed metadata"
      pattern: "TOP_LEVEL_ROUTES"
---

<objective>
Bootstrap the greenfield repo into a runnable frontend-only SPA workspace and freeze the Phase 1 routing/content contracts before shell work starts.

Purpose: Phase 1 cannot be executed safely without a real React/Vite/Tailwind/Vitest baseline and a single typed route source of truth.
Output: Installed app scaffold, Tailwind theme baseline, isolated `BrowserRouter` wrapper, route metadata for the four top-level screens, and a first routing smoke test.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/research/SUMMARY.md
@/Users/tkestkes/Dagmara/.planning/research/STACK.md
@/Users/tkestkes/Dagmara/.planning/research/PITFALLS.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Vite React workspace and baseline toolchain</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
package.json
vite.config.ts
src/main.tsx
src/styles/app.css
  </read_first>
  <files>
package.json
package-lock.json
index.html
tsconfig.json
tsconfig.app.json
tsconfig.node.json
vite.config.ts
eslint.config.js
public/vite.svg
src/App.tsx
src/index.css
src/assets/react.svg
src/main.tsx
src/vite-env.d.ts
src/styles/app.css
src/test/setup.ts
  </files>
  <action>Run `npm create vite@latest . -- --template react-ts` in the repo root, then install exactly these app dependencies: `react-router`, `tailwindcss`, `@tailwindcss/vite`, `@fontsource-variable/manrope`; install these dev dependencies if the template did not already add them: `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/user-event`. Remove template leftovers that do not belong in the CBDC demo (`src/App.tsx`, `src/index.css`, `src/assets/react.svg`, `public/vite.svg`). Update `vite.config.ts` to use `react()` and `tailwindcss()` and to expose `test.environment = "jsdom"` plus `test.setupFiles = ["./src/test/setup.ts"]`. Replace `src/main.tsx` so it imports `./styles/app.css` and renders `<AppRouter />` instead of the Vite counter demo. Create `src/styles/app.css` with `@import "tailwindcss";`, `@import "@fontsource-variable/manrope/wght.css";`, the exact UI token names from `01-UI-SPEC.md` (`--color-bg`, `--color-surface`, `--color-surface-muted`, `--color-text-strong`, `--color-text-muted`, `--color-border-soft`, `--color-accent`, `--color-danger`, `--radius-control`, `--radius-card`, `--shadow-card`), and base body/app rules that set the background to `#F5F1E8`, the font family to Manrope, and a full-height root canvas. Keep the app frontend-only per D-01 and do not add backend, API, or data-fetching code.</action>
  <acceptance_criteria>
- `package.json` contains `"react-router"`, `"tailwindcss"`, `"@tailwindcss/vite"`, `"@fontsource-variable/manrope"`, `"vitest"`, `"@testing-library/react"`, and `"@testing-library/user-event"`.
- `vite.config.ts` contains `react()`, `tailwindcss()`, `environment: "jsdom"`, and `setupFiles: ["./src/test/setup.ts"]`.
- `src/main.tsx` contains `import "./styles/app.css";` and `<AppRouter />`.
- `src/styles/app.css` contains `@import "tailwindcss";`, `@import "@fontsource-variable/manrope/wght.css";`, `--color-bg: #F5F1E8;`, and `--color-accent: #0F6CBD;`.
- `src/App.tsx`, `src/index.css`, `src/assets/react.svg`, and `public/vite.svg` do not exist after cleanup.
  </acceptance_criteria>
  <verify>
    <automated>npm install</automated>
  </verify>
  <done>Repo has a clean Vite/Tailwind/Vitest baseline with the approved theme tokens and no leftover starter-app demo assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the router contract, route metadata, and first route smoke tests</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/main.tsx
src/test/setup.ts
src/app/AppRouter.tsx
src/app/AppRoutes.tsx
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/pages/DashboardPage.tsx
src/pages/TransfersPage.tsx
src/pages/RatesPage.tsx
src/pages/SettingsPage.tsx
src/pages/NotFoundPage.tsx
src/pages/index.ts
src/test/renderApp.tsx
src/app/AppRouter.test.tsx
  </read_first>
  <files>
src/app/AppRouter.tsx
src/app/AppRoutes.tsx
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/pages/DashboardPage.tsx
src/pages/TransfersPage.tsx
src/pages/RatesPage.tsx
src/pages/SettingsPage.tsx
src/pages/NotFoundPage.tsx
src/pages/index.ts
src/test/renderApp.tsx
src/app/AppRouter.test.tsx
  </files>
  <behavior>
- Test 1: Rendering the route tree at `/` redirects to `/dashboard` and shows the dashboard stub title.
- Test 2: Rendering the route tree at `/rates` shows the `Обмен валют` stub title without requiring a page reload helper.
- Test 3: Unknown routes render the not-found stub route instead of crashing.
  </behavior>
  <action>Create `src/content/topLevelRoutes.ts` as the single typed source of truth for the four Phase 1 top-level routes from D-03 with these exact paths and labels: `/dashboard` -> `Главная`, `/transfers` -> `Переводы`, `/rates` -> `Обмен валют`, `/settings` -> `Настройки`. Include `pageTitle`, `supportingCopy`, `ctaLabel`, and `boundaryMode` fields now, using the exact copy from `01-UI-SPEC.md` for all four screens so later plans do not duplicate strings. Create `src/content/demoCopy.ts` with the exact badge label `Simulated demo`, the exact boundary line `Клиентская симуляция с фиктивными данными. Реального движения денег и официальных интеграций с центральными банками нет.`, the exact empty-state copy, and the exact not-found error copy from `01-UI-SPEC.md`. Isolate router setup into two files: `src/app/AppRouter.tsx` should be the only file importing `BrowserRouter`, while `src/app/AppRoutes.tsx` exports the `Routes` tree so a future `HashRouter` fallback is a one-file swap. In `AppRoutes`, redirect `/` to `/dashboard` with `<Navigate to="/dashboard" replace />` per D-02, define routes for `/dashboard`, `/transfers`, `/rates`, `/settings`, and `*`, and point them at temporary page stubs exported from `src/pages/index.ts`. Create `src/test/renderApp.tsx` with a MemoryRouter-based helper that can render `AppRoutes` at a chosen initial path. Write `src/app/AppRouter.test.tsx` first, then make it pass. Do not introduce `react-router-dom`; use `react-router` only.</action>
  <acceptance_criteria>
- `src/app/AppRouter.tsx` contains `BrowserRouter` and `<AppRoutes />`.
- `src/app/AppRoutes.tsx` contains `Navigate to="/dashboard" replace`, `path="/dashboard"`, `path="/transfers"`, `path="/rates"`, `path="/settings"`, and `path="*"`.
- `src/content/topLevelRoutes.ts` contains the exact nav labels `Главная`, `Переводы`, `Обмен валют`, and `Настройки`.
- `src/content/demoCopy.ts` contains the exact string `Simulated demo` and the exact canonical disclaimer line from `01-UI-SPEC.md`.
- `src/test/renderApp.tsx` contains `MemoryRouter`.
- `src/app/AppRouter.test.tsx` contains assertions for `/`, `/rates`, and unknown-route rendering.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/AppRouter.test.tsx --run</automated>
  </verify>
  <done>Phase 1 has a stable router/content contract, root redirect behavior is locked in, and route rendering can be tested independently from the future shell.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- src/app/AppRouter.test.tsx --run`
- `npm run build`
</verification>

<success_criteria>
- `npm install` succeeds with the approved frontend-only stack.
- `AppRouter` is the only `BrowserRouter` owner and `AppRoutes` owns the route tree.
- The four top-level routes, root redirect, and canonical demo-copy constants exist before shell implementation starts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-01-SUMMARY.md`
</output>
