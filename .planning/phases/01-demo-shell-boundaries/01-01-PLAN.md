---
phase: 01-demo-shell-boundaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - eslint.config.js
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - src/styles/app.css
  - src/test/setup.ts
autonomous: true
requirements:
  - NAVG-03
  - NAVG-04
user_setup: []
must_haves:
  truths:
    - User can open the app and see a Dagmara-branded desktop canvas instead of the default Vite starter screen.
    - User sees the approved background, typography, and centered page rhythm before feature-specific shell content is added.
    - User does not see live-banking, backend, or real-money claims on the initial screen.
  artifacts:
    - path: "package.json"
      provides: "Installable React 19, Vite 8, Tailwind 4, React Router 7, and Vitest baseline"
      contains: "\"react-router\""
    - path: "src/App.tsx"
      provides: "Neutral branded boot screen used until the router contract is wired"
      contains: "frontend-only demo shell"
    - path: "src/styles/app.css"
      provides: "Global shell tokens and base canvas styling from the UI contract"
      contains: "--color-bg"
    - path: "vite.config.ts"
      provides: "React, Tailwind, and jsdom/Vitest integration"
      contains: "setupFiles"
    - path: "src/test/setup.ts"
      provides: "Shared RTL cleanup hook for downstream route and shell tests"
      contains: "cleanup"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*<App />"
    - from: "src/main.tsx"
      to: "src/styles/app.css"
      via: "Global theme import"
      pattern: "\\./styles/app\\.css"
    - from: "vite.config.ts"
      to: "src/test/setup.ts"
      via: "Vitest setup registration"
      pattern: "setupFiles: \\[\"\\./src/test/setup\\.ts\"\\]"
---

<objective>
Safely bootstrap the repo into a runnable frontend-only Vite workspace without overwriting planning artifacts or repo metadata.

Purpose: Phase 1 cannot execute safely until the repository has a real React/Vite/Tailwind/Vitest baseline and a demo-safe starting screen.
Output: Preserved repo metadata, installable frontend toolchain, theme/test setup, and a branded temporary app root.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/research/SUMMARY.md
@/Users/tkestkes/Dagmara/.planning/research/STACK.md
@/Users/tkestkes/Dagmara/.planning/research/PITFALLS.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the app from a safe temp scaffold instead of touching the repo root</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
package.json
vite.config.ts
src/main.tsx
  </read_first>
  <files>
package.json
package-lock.json
index.html
tsconfig.json
tsconfig.app.json
tsconfig.node.json
vite.config.ts
eslint.config.js
src/main.tsx
src/vite-env.d.ts
  </files>
  <action>Per D-01 and the repo-safety checker note, do not run `npm create vite@latest . -- --template react-ts` in `/Users/tkestkes/Dagmara`. Instead create a temp directory scaffold with `mktemp -d`, run the non-interactive Vite scaffold there, and copy only the generated app baseline files back into the repo: `package.json`, `index.html`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `vite.config.ts`, `eslint.config.js`, `src/main.tsx`, and `src/vite-env.d.ts`. Leave `.planning/`, `AGENTS.md`, and `CLAUDE.md` untouched. Update `package.json` so the baseline includes `react`, `react-dom`, `react-router`, `tailwindcss`, `@tailwindcss/vite`, `@fontsource-variable/manrope`, `vite`, `@vitejs/plugin-react`, `vitest`, `jsdom`, `@testing-library/react`, and `@testing-library/user-event`, then run `npm install` in the repo root to generate the real `package-lock.json`. Configure `vite.config.ts` with `react()`, `tailwindcss()`, `test.environment = "jsdom"`, and `test.setupFiles = ["./src/test/setup.ts"]`. Do not copy Vite starter art or CSS (`public/vite.svg`, `src/assets/react.svg`, `src/index.css`).</action>
  <acceptance_criteria>
- `package.json` contains `"react-router"`, `"tailwindcss"`, `"@tailwindcss/vite"`, `"@fontsource-variable/manrope"`, `"vitest"`, `"@testing-library/react"`, and `"@testing-library/user-event"`.
- `vite.config.ts` contains `react()`, `tailwindcss()`, `environment: "jsdom"`, and `setupFiles: ["./src/test/setup.ts"]`.
- `.planning/`, `AGENTS.md`, and `CLAUDE.md` still exist after scaffolding.
- `public/vite.svg`, `src/assets/react.svg`, and `src/index.css` do not exist in the repo after the copy step.
  </acceptance_criteria>
  <verify>
    <automated>npm install</automated>
  </verify>
  <done>The repo has a safe, installable frontend baseline and the scaffold step cannot overwrite planning docs or repo metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the starter screen with a demo-safe branded entry surface and theme tokens</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/main.tsx
src/App.tsx
src/styles/app.css
src/test/setup.ts
  </read_first>
  <files>
src/main.tsx
src/App.tsx
src/styles/app.css
src/test/setup.ts
  </files>
  <action>Per D-08, D-09, and D-10, replace any Vite starter UI with a temporary branded screen that is clearly a frontend-only placeholder for the CBDC demo shell, not a live banking product. `src/main.tsx` must import `./styles/app.css` and render `<App />`. `src/App.tsx` should render one centered card-like entry surface with the project title, a short line that this is a frontend-only demo shell, and a note that navigation and routed screens arrive in the next plan; avoid router, rates, transfer widgets, or live-operation claims here. Create `src/styles/app.css` with `@import "tailwindcss";`, `@import "@fontsource-variable/manrope/wght.css";`, and the exact token names from `01-UI-SPEC.md` (`--color-bg`, `--color-surface`, `--color-surface-muted`, `--color-text-strong`, `--color-text-muted`, `--color-border-soft`, `--color-accent`, `--color-danger`, `--radius-control`, `--radius-card`, `--shadow-card`), then add body and `#root` rules that establish the full-height `#F5F1E8` canvas and centered desktop rhythm. Create `src/test/setup.ts` with RTL cleanup so downstream route and shell tests do not duplicate setup.</action>
  <acceptance_criteria>
- `src/main.tsx` contains `import "./styles/app.css";` and renders `<App />`.
- `src/App.tsx` contains a branded placeholder message and the phrase `frontend-only demo shell`.
- `src/styles/app.css` contains `@import "tailwindcss";`, `@import "@fontsource-variable/manrope/wght.css";`, `--color-bg: #F5F1E8;`, and `--color-accent: #0F6CBD;`.
- `src/test/setup.ts` contains RTL cleanup registration.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>The app boots into a demo-safe branded baseline with the approved desktop token contract and test setup in place.</done>
</task>

</tasks>

<verification>
Run:
- `npm exec tsc --noEmit`
- `npm run build`
</verification>

<success_criteria>
- The repository installs without overwriting `.planning/`, `AGENTS.md`, or `CLAUDE.md`.
- The first rendered screen is a Dagmara-branded placeholder, not the Vite starter app.
- The global theme and test harness are ready for router and shell work in the next plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-01-SUMMARY.md`
</output>
