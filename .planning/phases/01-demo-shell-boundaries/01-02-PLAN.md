---
phase: 01-demo-shell-boundaries
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/AppRoutes.tsx
  - src/shell/AppShell.tsx
  - src/shell/SidebarNav.tsx
  - src/shell/TopHeader.tsx
  - src/shell/GlobalSearchStub.tsx
  - src/shell/NotificationButton.tsx
  - src/shell/PageContainer.tsx
  - src/shell/ShellCard.tsx
  - src/shell/StatusBadge.tsx
  - src/shell/icons.tsx
  - src/test/renderApp.tsx
  - src/shell/AppShell.test.tsx
autonomous: true
requirements:
  - NAVG-01
  - NAVG-02
  - NAVG-03
  - NAVG-04
user_setup: []
must_haves:
  truths:
    - Users can see one persistent desktop shell with a left sidebar, top header, and centered main content.
    - Users can see the four required top-level sections and move between them with route-based SPA navigation.
    - The shared header shows `Здравствуйте, Дагмара`, a faux-search field, a notifications control, and the shell-level `Simulated demo` badge on every route.
  artifacts:
    - path: "src/shell/AppShell.tsx"
      provides: "Persistent shell layout with Outlet"
      contains: "Outlet"
    - path: "src/shell/SidebarNav.tsx"
      provides: "Mapped route navigation with active-state treatment"
      contains: "NavLink"
    - path: "src/shell/TopHeader.tsx"
      provides: "Greeting, search stub, notifications button, and badge"
      contains: "Здравствуйте, Дагмара"
    - path: "src/shell/PageContainer.tsx"
      provides: "Centered 1200px content container"
      contains: "1200"
    - path: "src/shell/AppShell.test.tsx"
      provides: "Regression coverage for persistent shell and nav behavior"
      contains: "toHaveValue(\"demo\")"
  key_links:
    - from: "src/app/AppRoutes.tsx"
      to: "src/shell/AppShell.tsx"
      via: "Layout route element"
      pattern: "element={<AppShell />}"
    - from: "src/shell/SidebarNav.tsx"
      to: "src/content/topLevelRoutes.ts"
      via: "Mapped route metadata"
      pattern: "TOP_LEVEL_ROUTES"
    - from: "src/shell/TopHeader.tsx"
      to: "src/content/demoCopy.ts"
      via: "Shell badge label"
      pattern: "Simulated demo"
    - from: "src/shell/PageContainer.tsx"
      to: "01-UI-SPEC.md"
      via: "Centered width and spacing contract"
      pattern: "1200px"
---

<objective>
Implement the persistent desktop shell and wire it into the Phase 1 route tree so navigation, header chrome, and content constraints are shared across all top-level screens.

Purpose: NAVG-01 through NAVG-04 all depend on a stable shell that does not remount or imply backend behavior.
Output: `AppShell`, sidebar/header primitives, `PageContainer`, and automated shell navigation tests.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/research/STACK.md
@/Users/tkestkes/Dagmara/.planning/research/PITFALLS.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-01-SUMMARY.md

<interfaces>
Expected route metadata from `src/content/topLevelRoutes.ts`:
```ts
export interface TopLevelRoute {
  path: "/dashboard" | "/transfers" | "/rates" | "/settings";
  navLabel: "Главная" | "Переводы" | "Обмен валют" | "Настройки";
  pageTitle: string;
  supportingCopy: string;
  ctaLabel: string;
  boundaryMode: "intro-card" | "inline-helper";
}
export const TOP_LEVEL_ROUTES: readonly TopLevelRoute[];
```

Expected demo copy contract from `src/content/demoCopy.ts`:
```ts
export const SHELL_BADGE_LABEL = "Simulated demo";
export const PHASE_BOUNDARY_COPY =
  "Клиентская симуляция с фиктивными данными. Реального движения денег и официальных интеграций с центральными банками нет.";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build shell primitives for sidebar, header, and content framing</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/app/AppRoutes.tsx
src/shell/AppShell.tsx
src/shell/SidebarNav.tsx
src/shell/TopHeader.tsx
src/shell/GlobalSearchStub.tsx
src/shell/NotificationButton.tsx
src/shell/PageContainer.tsx
src/shell/ShellCard.tsx
src/shell/StatusBadge.tsx
src/shell/icons.tsx
  </read_first>
  <files>
src/shell/AppShell.tsx
src/shell/SidebarNav.tsx
src/shell/TopHeader.tsx
src/shell/GlobalSearchStub.tsx
src/shell/NotificationButton.tsx
src/shell/PageContainer.tsx
src/shell/ShellCard.tsx
src/shell/StatusBadge.tsx
src/shell/icons.tsx
  </files>
  <behavior>
- Test 1: The shell renders the four nav labels from route metadata plus the shared header greeting and search placeholder.
- Test 2: The notifications control exposes `aria-label="Уведомления"` and does not render a badge or dropdown.
- Test 3: The shell badge label is exactly `Simulated demo`.
  </behavior>
  <action>Create the local shell primitives required by D-01, D-05, D-06, D-07, D-09, and D-10. `AppShell.tsx` should own a desktop layout with a fixed `264px` sidebar column and a content column that includes an `80px` header row and an Outlet area. `SidebarNav.tsx` must map over `TOP_LEVEL_ROUTES` with `NavLink` from `react-router`; each item must use the exact labels from metadata, `48px` target height, `16px` horizontal padding, `12px` icon gap, `16px` radius, and a visible active-state treatment with an accent-tinted background plus a `3px` accent rail. `TopHeader.tsx` must render the exact greeting `Здравствуйте, Дагмара`, the `GlobalSearchStub` with placeholder `Поиск по demo`, the `NotificationButton` with `aria-label="Уведомления"`, and the `StatusBadge` using the exact shell label from `demoCopy.ts`. Build outline-only icons in `src/shell/icons.tsx` with rounded caps/joins and `strokeWidth={1.75}`. Keep search and notifications display-first only: no dropdowns, unread counters, timestamps, or server-shaped copy.</action>
  <acceptance_criteria>
- `src/shell/SidebarNav.tsx` contains `NavLink`, `TOP_LEVEL_ROUTES`, `Главная`, `Переводы`, `Обмен валют`, and `Настройки`.
- `src/shell/TopHeader.tsx` contains `Здравствуйте, Дагмара` and `Поиск по demo`.
- `src/shell/NotificationButton.tsx` contains `aria-label="Уведомления"` and does not contain `badge`, `count`, or `dropdown`.
- `src/shell/StatusBadge.tsx` or `src/shell/TopHeader.tsx` contains the exact string `Simulated demo`.
- `src/shell/PageContainer.tsx` or `src/shell/AppShell.tsx` contains `1200` for the max-width contract and `760` for the narrative column contract.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>All shell primitives exist with the approved desktop proportions, copy, and display-first constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the shell as the persistent layout route and prove SPA navigation behavior</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/app/AppRoutes.tsx
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/test/renderApp.tsx
src/shell/AppShell.tsx
src/shell/SidebarNav.tsx
src/shell/TopHeader.tsx
src/shell/PageContainer.tsx
src/shell/AppShell.test.tsx
  </read_first>
  <files>
src/app/AppRoutes.tsx
src/test/renderApp.tsx
src/shell/AppShell.test.tsx
  </files>
  <behavior>
- Test 1: Route rendering at `/dashboard` shows the shell plus the dashboard page stub inside the content column.
- Test 2: Typing `demo` into the search stub and then clicking `Обмен валют` keeps the input value, proving the shell did not remount on route change.
- Test 3: The main content container keeps the centered `1200px` width contract while the shell badge and header stay visible after navigation.
  </behavior>
  <action>Update `src/app/AppRoutes.tsx` so the top-level route tree wraps all child routes in `element={<AppShell />}` per D-01 and D-04, while keeping the root redirect from Plan 01 intact. `AppShell` must render `<Outlet />` inside a centered content region powered by `PageContainer`, with `32px` horizontal padding, `32px` top padding, `48px` bottom padding, and an inner narrative width capped at `760px` inside the broader `1200px` page canvas from D-10. Expand `src/test/renderApp.tsx` so tests can render `AppRoutes` at specific routes and interact with the shell. Write `src/shell/AppShell.test.tsx` before implementation, then make it pass with assertions for the four nav labels, greeting, search placeholder, `aria-label="Уведомления"`, shell badge visibility, and the search-value persistence check after clicking a different nav item. This task must keep the route changes inside `react-router`; do not add any `window.location` mutation logic or tab-state navigation.</action>
  <acceptance_criteria>
- `src/app/AppRoutes.tsx` contains `element={<AppShell />}` and nested child routes for `/dashboard`, `/transfers`, `/rates`, `/settings`, and `*`.
- `src/shell/AppShell.tsx` contains `<Outlet />`.
- `src/shell/AppShell.test.tsx` contains `toHaveValue("demo")` after a nav click and assertions for `Здравствуйте, Дагмара`, `Поиск по demo`, and `aria-label="Уведомления"`.
- `src/shell/PageContainer.tsx` or `src/shell/AppShell.tsx` contains a `1200px` width constraint and the `32px`/`48px` spacing contract from `01-UI-SPEC.md`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/shell/AppShell.test.tsx --run</automated>
  </verify>
  <done>The Phase 1 shell is persistent, route-based, and locked to the approved desktop layout contract.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- src/shell/AppShell.test.tsx --run`
- `npm run build`
</verification>

<success_criteria>
- Sidebar, header, and centered main content render from one persistent layout route.
- Navigation uses `react-router` links and preserves shell-local state during route changes.
- The four required top-level sections and the required header chrome are visible on every route.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-02-SUMMARY.md`
</output>
