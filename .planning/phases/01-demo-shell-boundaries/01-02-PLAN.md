---
phase: 01-demo-shell-boundaries
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/App.tsx
  - src/app/AppRouter.tsx
  - src/app/AppRoutes.tsx
  - src/content/topLevelRoutes.ts
  - src/content/demoCopy.ts
  - src/pages/DashboardPage.tsx
  - src/pages/TransfersPage.tsx
  - src/pages/RatesPage.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/NotFoundPage.tsx
  - src/pages/index.ts
  - src/test/renderApp.tsx
  - src/app/AppRouter.test.tsx
autonomous: true
requirements:
  - NAVG-03
user_setup: []
must_haves:
  truths:
    - User entering `/` lands on `Главная` automatically instead of a blank root screen.
    - User can open `/dashboard`, `/transfers`, `/rates`, and `/settings` and see the approved Russian section titles without a full browser reload.
    - Unknown URLs show a controlled in-app fallback state instead of a browser error or empty page.
  artifacts:
    - path: "src/App.tsx"
      provides: "App root switched from the temporary placeholder to the router entry"
      contains: "AppRouter"
    - path: "src/app/AppRouter.tsx"
      provides: "BrowserRouter wrapper isolated to one file for low-cost router swaps"
      contains: "BrowserRouter"
    - path: "src/app/AppRoutes.tsx"
      provides: "Shared route tree with root redirect and top-level paths"
      contains: "Navigate to=\"/dashboard\""
    - path: "src/content/topLevelRoutes.ts"
      provides: "Single source of truth for Phase 1 route metadata"
      contains: "navLabel"
    - path: "src/app/AppRouter.test.tsx"
      provides: "Regression coverage for root redirect, known routes, and unknown routes"
      contains: "/rates"
  key_links:
    - from: "src/App.tsx"
      to: "src/app/AppRouter.tsx"
      via: "App root handoff from bootstrap view to router"
      pattern: "AppRouter"
    - from: "src/app/AppRouter.tsx"
      to: "src/app/AppRoutes.tsx"
      via: "Provider isolation for BrowserRouter"
      pattern: "<BrowserRouter>.*<AppRoutes />"
    - from: "src/app/AppRoutes.tsx"
      to: "src/content/topLevelRoutes.ts"
      via: "Typed route metadata contract"
      pattern: "TOP_LEVEL_ROUTES"
---

<objective>
Freeze the Phase 1 router and copy contracts before the persistent shell is built.

Purpose: Splitting route/content work from bootstrap keeps Phase 1 executable, reduces file scope, and locks the user-facing section contract early.
Output: Router entrypoint, route metadata, canonical demo copy constants, page stubs, and first route smoke tests.
</objective>

<execution_context>
@/Users/tkestkes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tkestkes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tkestkes/Dagmara/AGENTS.md
@/Users/tkestkes/Dagmara/CLAUDE.md
@/Users/tkestkes/Dagmara/.planning/PROJECT.md
@/Users/tkestkes/Dagmara/.planning/REQUIREMENTS.md
@/Users/tkestkes/Dagmara/.planning/ROADMAP.md
@/Users/tkestkes/Dagmara/.planning/STATE.md
@/Users/tkestkes/Dagmara/.planning/research/SUMMARY.md
@/Users/tkestkes/Dagmara/.planning/research/STACK.md
@/Users/tkestkes/Dagmara/.planning/research/PITFALLS.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
@/Users/tkestkes/Dagmara/.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the bootstrap placeholder with the Phase 1 router and copy contracts</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/App.tsx
src/app/AppRouter.tsx
src/app/AppRoutes.tsx
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/pages/DashboardPage.tsx
src/pages/TransfersPage.tsx
src/pages/RatesPage.tsx
src/pages/SettingsPage.tsx
src/pages/NotFoundPage.tsx
src/pages/index.ts
  </read_first>
  <files>
src/App.tsx
src/app/AppRouter.tsx
src/app/AppRoutes.tsx
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/pages/DashboardPage.tsx
src/pages/TransfersPage.tsx
src/pages/RatesPage.tsx
src/pages/SettingsPage.tsx
src/pages/NotFoundPage.tsx
src/pages/index.ts
  </files>
  <action>Per D-02, D-03, D-07, and D-08, replace the temporary `src/App.tsx` bootstrap screen with `<AppRouter />`. Create `src/app/AppRouter.tsx` as the only file importing `BrowserRouter`, and create `src/app/AppRoutes.tsx` as the route-tree owner so a future `HashRouter` fallback remains a one-file swap. In `AppRoutes`, redirect `/` to `/dashboard` with `<Navigate to="/dashboard" replace />`, define routes for `/dashboard`, `/transfers`, `/rates`, `/settings`, and `*`, and point them at temporary page stubs from `src/pages/index.ts`. Create `src/content/topLevelRoutes.ts` as the single typed source of truth for the four Phase 1 routes with the exact labels and titles from `01-UI-SPEC.md`, plus `supportingCopy`, `ctaLabel`, and `boundaryMode`. Create `src/content/demoCopy.ts` with the exact shell badge label `Simulated demo`, the exact canonical disclaimer line, the empty-state copy, and the not-found error copy from `01-UI-SPEC.md`. Keep these page components stub-only for now: no shell layout, no data widgets, no live-banking claims.</action>
  <acceptance_criteria>
- `src/App.tsx` renders `AppRouter`.
- `src/app/AppRoutes.tsx` contains `Navigate to="/dashboard" replace`, `path="/dashboard"`, `path="/transfers"`, `path="/rates"`, `path="/settings"`, and `path="*"`.
- `src/content/topLevelRoutes.ts` contains the exact nav labels `Главная`, `Переводы`, `Обмен валют`, and `Настройки`.
- `src/content/demoCopy.ts` contains the exact string `Simulated demo` and the canonical disclaimer line from `01-UI-SPEC.md`.
- `src/pages/NotFoundPage.tsx` exists as an in-app fallback stub.
  </acceptance_criteria>
  <verify>
    <automated>npm exec tsc --noEmit</automated>
  </verify>
  <done>The app root now enters a stable router tree with locked route metadata and canonical demo-copy constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route smoke tests and a reusable router-aware render helper</name>
  <read_first>
AGENTS.md
CLAUDE.md
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/01-demo-shell-boundaries/01-CONTEXT.md
.planning/phases/01-demo-shell-boundaries/01-RESEARCH.md
.planning/phases/01-demo-shell-boundaries/01-UI-SPEC.md
src/app/AppRouter.tsx
src/app/AppRoutes.tsx
src/content/topLevelRoutes.ts
src/content/demoCopy.ts
src/test/renderApp.tsx
src/app/AppRouter.test.tsx
  </read_first>
  <files>
src/test/renderApp.tsx
src/app/AppRouter.test.tsx
  </files>
  <behavior>
- Test 1: Rendering the route tree at `/` redirects to `/dashboard` and shows the dashboard stub title.
- Test 2: Rendering the route tree at `/rates` shows the `Обмен валют` stub title without a browser reload.
- Test 3: Unknown routes render the in-app not-found stub instead of crashing.
  </behavior>
  <action>Create `src/test/renderApp.tsx` with a `MemoryRouter`-based helper that can render `AppRoutes` at a chosen initial path. Write `src/app/AppRouter.test.tsx` first, then make it pass with assertions for `/`, `/rates`, and an unknown route. Keep the tests focused on D-02 and D-03 routing behavior only; shell persistence, layout, and boundary treatments belong to later plans.</action>
  <acceptance_criteria>
- `src/test/renderApp.tsx` contains `MemoryRouter`.
- `src/app/AppRouter.test.tsx` contains assertions for `/`, `/rates`, and unknown-route rendering.
- The dashboard and rates assertions use the exact Russian titles from `topLevelRoutes.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/AppRouter.test.tsx --run</automated>
  </verify>
  <done>Phase 1 has a testable route contract for the root redirect, known sections, and in-app fallback behavior.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- src/app/AppRouter.test.tsx --run`
- `npm run build`
</verification>

<success_criteria>
- `/` redirects to `/dashboard`.
- The four top-level routes and the in-app fallback render through `react-router`.
- Route metadata and canonical demo-copy constants are frozen before shell implementation starts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-shell-boundaries/01-demo-shell-boundaries-02-SUMMARY.md`
</output>
